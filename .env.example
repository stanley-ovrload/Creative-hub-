# Path to the SQLite job store, used by both the console (Next.js) and
# the worker (npm run worker). Relative paths resolve against the repo
# root. Defaults to data/jobs.db if unset.
DATABASE_PATH=data/jobs.db
